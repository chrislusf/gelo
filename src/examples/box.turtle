
command rsquare {len dep} {
    if [/0? $dep] then {
        repeat 4 {
            forward $len
            right
            rsquare [* $len 0.4] [- $dep 1]
        }
    }
}

jump-to -50 50
rsquare 200 5
